
// stdafx.cpp : 표준 포함 파일만 들어 있는 소스 파일입니다.
// JakeSee2.pch는 미리 컴파일된 헤더가 됩니다.
// stdafx.obj에는 미리 컴파일된 형식 정보가 포함됩니다.

#include "stdafx.h"


u32_t __avoid_symbol_not_exist_2_dz1_mfc__ = 0;

/*
void {some_list_ctrl}::OnNMClick{some_list_instance}(NMHDR *pNMHDR, LRESULT *pResult)
{
	int cnt = {some_list_instance_variable}.GetSelectedCount();
	if (cnt == 0)
	{
		// no row selected
	}
}

void {some_list_ctrl}::OnLvnItemchanged{some_list_instance}(NMHDR *pNMHDR, LRESULT *pResult)
{
	if ((!(pNMLV->uOldState & LVIS_FOCUSED) && (pNMLV->uNewState & LVIS_FOCUSED)) ||
		((pNMLV->uOldState & LVIS_FOCUSED) && !(pNMLV->uNewState & LVIS_FOCUSED)))
	{	// some item get/lost focus
		int idx = pNMLV->iItem;
		int cnt = {some_list_instance_variable}.GetSelectedCount();
		if (cnt == 0)
		{
			// no row selected
		}
		else
		{	// one or more rows selected 
			if (cnt == 1)
			{
				// just one row selected
			}
			else
			{
				// many rows selected
			}
		}
	}
}
*/