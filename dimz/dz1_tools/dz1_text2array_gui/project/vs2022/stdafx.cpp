// stdafx.cpp : 표준 포함 파일을 포함하는 소스 파일입니다.
// dz1_text2array_gui.pch는 미리 컴파일된 헤더가 됩니다.
// stdafx.obj는 미리 컴파일된 형식 정보를 포함합니다.

#include "stdafx.h"

#if 0
#ifdef _WIN32
#ifdef _WIN64
#error "_WIN32 + _WIN64"
#else
#error "_WIN32 Only"
#endif
#else
#ifdef _WIN64
#error "_WIN64"
#else
#error "Nothing defined"
#endif
#endif
#endif