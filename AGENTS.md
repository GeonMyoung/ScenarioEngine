# 경찰청 표준적합성시험 개발 작업공간

- 이 저장소는 `ScenarioEngine` 공통 엔진, 표준별 시험 모듈, 에뮬레이터와 공통 SDK 소스를 함께 관리한다.
- 사용자와 프로젝트 문서는 한국어로 소통한다. 파일명, 식별자, 표준 번호는 유지한다.
- `README.md`, `dependencies.lock.json`, `doc/import-source-manifest.json`을 기준으로 작업 범위와 의존성을 확인한다.
- 원본에는 미커밋 변경이 있었으며, 초기 통합은 해당 작업 파일의 스냅샷이다. 기존 모듈별 Git 이력은 이 저장소에 합치지 않았다.
- prj22는 구조와 개선 사례를 비교하는 참고 프로젝트다. 두 프로젝트의 시험 항목과 메시지 계약을 동일하다고 가정하지 않는다.
- SDK는 이 저장소 안의 실제 소스와 `.deps/sdk`를 사용한다. 개인 PC의 `_SDK_` 정션이나 전역 환경 변수에 의존하는 변경을 추가하지 않는다.
- 기존 C 소스에는 CP949와 UTF-8이 섞여 있다. 수정 대상의 인코딩과 줄바꿈을 보존하고 관련 없는 파일을 일괄 변환하지 않는다.
- 생성된 `.c`, `.h`, `.tdo`도 버전 관리한다. 기본 빌드는 저장된 생성 소스를 사용한다. `.tdc`/`.tjsonc` 수정 시 생성 도구를 준비하고 해당 파일의 생성 결과만 검토한다.
- 작은 문서 변경에는 테스트를 추가하지 않는다. 빌드 설정이나 의존성 변경은 `tools/validate-workspace.ps1`과 해당 빌드로 확인한다.
- 프로그램 실행은 실제 시험 대상에 통신할 수 있다. 빌드 확인과 실제 표준적합성시험 결과를 구분한다.
- 경4 분석·설계·구현 범위는 `doc/gyeong4/2026-v2.1/development-scope.md`를 따른다. DB 연계 3건과 대상 시스템 내부 업무 구현은 제외하고 외부 API 및 필수 시험 지원은 유지한다. 포함 범위와 규격·시험 접점 확인 조건을 구분하며 새 사용자 지시가 이 기준보다 우선한다.
- 새 요청마다 clone/worktree를 만들지 않는다. 기존 작업 환경을 먼저 확인해 재사용하고, 동시 독립 수정이 필요할 때만 추가 환경을 사용한다. 기본 폴더를 포함해 원칙적으로 2개 이하를 유지한다. 읽기 전용 검토는 커밋을 고정하며 다른 작업의 체크아웃·인덱스·미커밋 변경을 건드리지 않는다.
- 사용자 지시가 이 파일보다 우선한다.

## 업무 연동 적용 조건

- 기본은 일반 작업이다. 질문·현황 조회·분석·수정은 요청한 범위와 필요한 검증을 수행한다. 이 이유만으로 Riido 업무를 찾거나 생성하고, 실행 등록·중앙 인계·commit/push/PR을 시작하지 않는다. 현황 조회에 필요한 읽기 도구는 바로 사용한다.
- 사용자가 ScenarioEngine의 Riido 업무를 지정해 착수/계속하도록 하거나, 현재 대화에서 그 업무를 이미 연동해 진행 중이거나, GitHub 제출·Riido 반영을 요청하면 `scenarioengine-workflow` 스킬을 사용한다. 번호나 서비스 이름을 언급한 질문만으로 연동을 시작하지 않는다. 스킬 목록에 없으면 [저장소의 SKILL.md](skills/scenarioengine-workflow/SKILL.md)를 읽는다.
- 연동 업무는 착수와 제출 시 필요한 절차만 적용한다. 동일 업무의 중간 수정은 기존 실행으로 이어가며, 변경된 결과를 묶어 제출한다. 스킬·`doc/development-workflow.md`의 상세 절차는 연동 또는 운영 절차 자체를 다룰 때 읽는다.
- 연동 업무의 제출·중앙 검토·Riido 결과 확인은 사용자가 재요청하지 않아도 이어간다. GitHub만 요청하면 그 범위로 끝내고, Riido 기록만 요청하면 기존 산출물을 사용한다. 로컬만/분석만/커밋 금지/반영 보류 등 명시한 범위를 우선한다.
- Riido 쓰기는 중앙 검토 작업이 맡는다. 중앙은 실제 변경과 완료 조건을 변경 규모에 맞게 검토하고 저장값을 확인한다. 전달 성공·검토 수용·Riido 완료를 구분한다. 주기 조회는 사용자 재요청 전까지 중지한다.
